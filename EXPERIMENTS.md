# 实验报告 - feat/all‑docs‑win256‑e6‑lr1e5 (2025‑12‑06)

### 📆 实验时间  
2025‑12‑06

---

### 🔧 实验背景与目标

本次实验的目标是通过不同的 **数据增强**（`augment1000`）、 **训练设置的调整**（如窗口大小、学习率、batch size 等）来提升模型的 **macro-F1** 和 **official style score**。同时，解决了 **读取 docs 文件代码错误**，并且进行了 **AllDocs 数据的正确读取**

---

### 🧪 实验步骤与改动

#### 1. **修正读取 Docs 文件的代码错误**
在最初的实验中，发现读取 `docs.json` 文件的代码存在问题，导致训练过程中模型未能正确读取所有文档。这个问题影响了训练结果，并且训练的文档量较小。

##### **改动思路和原理**：
- **问题**：原代码在读取文档时没有完全加载所有文档数据，导致每次训练只使用部分文档，未能利用完整的训练数据。
- **改动**：修正了数据加载过程，确保所有文档都被正确加载到模型中，确保 **每个样本都能够从全部文档中学习**。
- **代码改动**：调整了 `train_docs_json` 和 `train_jsonl` 文件的读取部分，确保 `docs.json` 的所有条目被正确加载并传递给模型。

##### **训练结果**：
- **dev 上的 macro-F1 提升**：修正了读取错误后，训练使用了完整数据集，模型在 **dev 集** 上的 **macro-F1** 从 **0.6800** 提升至 **0.7020**，**official score** 达到 **0.6212**，效果有了显著提升。

#### 2. **生成 `augment1000` 数据**
为进一步提高模型的泛化能力，我决定利用大模型生成 **增强数据集**，包括 1000 条增强问题和文档。

##### **生成思路**：
- 使用 **大模型**（如 GPT 或 Qwen3）生成与训练集问题相关的 **增强问题** 和 **文档**。
- 每条生成的文档都保持与原文档相关的上下文，同时提供更多的样本多样性，有助于模型学习更广泛的语言模式。
- **文档数量**：生成了 1000 条文档和 1000 条增强问题，使得训练集更加丰富多样。

##### **生成的数据**：
- **增强问题**：通过 `augment_questions_to_1000.py` 生成了 1000 个新的问题。
- **增强文档**：通过 `agument_docs.py` 生成了 1000 个新的文档。

##### **生成结果**：
- 在使用增强数据集后，模型的 **训练性能有所提升**，但 **official score** 不如原始数据集的结果。

#### 3. **调整 AllDocs 数据读取方式**
为了更好地利用所有文档，我对 `AllDocs` 训练代码进行了修改，确保 **每个文档都能被完整读取**，并且不会丢失信息。

##### **改动思路与原理**：
- **问题**：最初的代码无法正确处理所有文档。使用 `chunk` 方式时，只取了前几个窗口，未能完整读取文档的内容。
- **改动**：调整了训练代码，使得每个文档可以 **循环读取**，直到文档的所有内容都被模型学习。
- **新实现**：使用 **`max_windows=12`**，确保每道问题都能加载最多 12 个窗口，并且保证窗口大小和stride的平衡。

##### **训练结果**：
- **dev 上的 macro-F1 提升至** **0.7020**，**official score** 稳定在 **0.6212**。
- **数据增强效果**：使用增强数据集的结果不如原始数据集。**official score** 在使用增强数据时出现了 **下降**，表明数据增强的质量对模型的提升有一定影响。

#### 4. **总结与下一步计划**
通过本周的实验，我们能够看到数据增强、代码修复、窗口设置等方面的改进所带来的性能变化。以下是 **本周实验总结**：

- **修正代码**：确保文档的完整加载，解决了原始代码未能正确读取全部文档的问题，提升了 **model generalization**。
- **数据增强**：生成增强数据后，虽然增强文档丰富了训练数据，但由于增强数据的质量可能不高，导致在 **official score** 上有所下降。下次可以改进增强数据生成方式，确保文档和问题的质量。
- **训练优化**：通过调整 **window、max_windows 和 batch_size** 等超参数，使模型在较少的训练轮次下获得了更好的泛化能力。

---

### 📊 最终结果 (dev 集)

| Epoch | train loss (avg) | dev best‑thr | subset-acc | micro-F1 | macro-F1 |
|---|---|---|---|---|---|
| 1 | 0.1036 | 0.53 | 0.3875 | 0.6819 | 0.6807 |
| 2 | 0.0000 | 0.33 | 0.3825 | 0.6811 | 0.6801 |
| 3 | 0.0000 | 0.25 | 0.3800 | 0.6811 | 0.6801 |
| 4 | 0.0026 | 0.34 | 0.3750 | 0.6853 | 0.6837 |
| 5 | 0.0001 | 0.26 | 0.3850 | 0.6892 | 0.6878 |
| 6 | 0.0000 | 0.48 | 0.3800 | 0.6826 | 0.6813 |

**Official-style average score**: **0.6212**  
**Post-calibration micro-F1 / macro-F1**: **0.7029 / 0.7020**

---

### ✅ 经验与结论总结

1. **配置优化**：
   - 通过 **max_windows=12** 和 **学习率调整**，确保了模型能够处理更丰富的上下文信息，提高了 **dev macro-F1** 和 **official score**。
   - 启用 **AMP 和梯度裁剪** 显著减少了显存占用，并稳定了训练。

2. **数据增强的影响**：
   - 虽然使用了增强数据集，但结果表明 **增强数据** 对 **官方得分** 的提升效果有限，可能是由于数据质量或多样性不足。
   - 下次实验可以通过改进数据增强的生成过程来进一步优化。

---

### 🔜 下一步计划与待尝试的方向

1. **进一步的阈值优化**：对当前 **dev_predictions** 进行再次优化，寻找 **最优阈值**，提升 **official score**。
2. **模型集成**：尝试对不同训练配置或多个模型的预测结果进行集成，检查 **ensemble** 方法能否进一步提升 **F1 score**。
3. **更高质量的数据增强**：采用 **更强的生成模型**，如 Qwen3 或其他更大的模型，生成更有质量的训练样本。
4. **进一步的正则化与超参数微调**：增加 **Dropout** 或 **weight decay** 来进一步防止过拟合，并检查 **训练稳定性**。

---

### 📊 实验总表

| ID | Backbone | Train 数据 | Epochs | Batch | micro-F1 | macro-F1 | Official score |
|---|---|---|---|---|---|---|---|
| A | BERT-base | 原始 Q (200) + 原始 docs (181) | 3 | 1 | 0.6365 | 0.6370 | 0.5387 |
| B | BERT-base | 增强 Q (1000) + 增强 docs (3000) | 3 | 4 | 0.6244 | 0.6218 | 0.5387 |
| C | BERT-base | 增强 Q (1000) + 增强 docs (3000) | 3 | 1 | 0.6216 | 0.6201 | 0.5288 |
| D | DeBERTa-v3-base | 原始 Q (200) + 原始 docs (181) | 3 | 2 | 0.6401 | 0.6404 | 0.5413 |
| E | DeBERTa-v3-base | 增强 Q (1000) + 增强 docs (3000) | 3 | 2 | 0.5652 | 0.5645 | 0.4875 |
| F | DeBERTa-v3-base | AllDocs（chunk） | 6 | 1 | 0.7029 | 0.7020 | 0.6212 |

---

这个是**详细的实验报告**，记录了每个步骤的改动、实验设定、结果以及分析。你可以直接将其复制到你的 `EXPERIMENTS.md` 文件中。如果有任何部分需要修改或补充，请告诉我！
